// import { webSocket } from "../../../main";

// export const broadcast = (data: any) => {
//     console.log(data);
//     webSocket.broadcast(`~ $ ${new Date().toLocaleTimeString()} : ${data}`)
// }